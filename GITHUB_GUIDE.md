# 📤 КАК ОТПРАВИТЬ ПРОЕКТ НА GITHUB И СДЕЛАТЬ ЕГО ПУБЛИЧНЫМ

## 🔷 ЧАСТЬ 1: ПОДГОТОВКА К GITHUB

### Этап 1.1: Создание аккаунта на GitHub (если нет)
1. Перейдите на https://github.com
2. Нажмите "Sign up"
3. Заполните данные: email, пароль, имя пользователя
4. Подтвердите email

### Этап 1.2: Установка Git
**Для Windows:**
1. Скачайте Git с https://git-scm.com/download/win
2. Установите с настройками по умолчанию
3. Проверьте установку в PowerShell:
```powershell
git --version
```

### Этап 1.3: Настройка Git
**В PowerShell выполните:**

```powershell
# Настройка имени пользователя
git config --global user.name "ВашеИмяНаGitHub"

# Настройка email
git config --global user.email "ваш.email@example.com"

# Проверка настроек
git config --list
```

---

## 🔷 ЧАСТЬ 2: ИНИЦИАЛИЗАЦИЯ РЕПОЗИТОРИЯ

### Этап 2.1: Переход в папку проекта
```powershell
cd C:\Users\user\Projects\sber-auto-ml
```

### Этап 2.2: Инициализация Git репозитория
```powershell
git init
```
**Результат:** Создана скрытая папка `.git`

### Этап 2.3: Проверка статуса
```powershell
git status
```
**Результат:** Увидите список неотслеживаемых файлов

---

## 🔷 ЧАСТЬ 3: НАСТРОЙКА .gitignore

### Этап 3.1: Создание файла .gitignore
Создайте файл `.gitignore` в корне проекта с содержимым:

```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Jupyter
.ipynb_checkpoints/

# Data files (не загружаем большие CSV)
data/*.csv
data/*.pkl

# Модели (не загружаем обученные модели)
models/*.pkl

# Системные файлы
.DS_Store
Thumbs.db
```

### Этап 3.2: Добавление .gitignore
```powershell
git add .gitignore
git commit -m "Add .gitignore"
```

---

## 🔷 ЧАСТЬ 4: ДОБАВЛЕНИЕ ФАЙЛОВ ПРОЕКТА

### Этап 4.1: Добавление всех файлов
```powershell
git add .
```

### Этап 4.2: Проверка добавленных файлов
```powershell
git status
```

### Этап 4.3: Первый коммит
```powershell
git commit -m "Initial commit: ML project for SberAutoSubscription"
```

---

## 🔷 ЧАСТЬ 5: СОЗДАНИЕ РЕПОЗИТОРИЯ НА GITHUB

### Этап 5.1: Создание нового репозитория
1. Зайдите на GitHub
2. Нажмите "+" в правом верхнем углу → "New repository"
3. Заполните:
   - **Repository name:** sber-auto-ml
   - **Description:** ML project for SberAutoSubscription prediction
   - **Public** (выберите этот вариант для публичного репозитория)
   - Не отмечайте "Initialize with README" (у нас уже есть README)
4. Нажмите "Create repository"

### Этап 5.2: Копирование ссылки
После создания репозитория скопируйте URL (будет выглядеть примерно так):
```
https://github.com/ВашеИмя/sber-auto-ml.git
```

---

## 🔷 ЧАСТЬ 6: ОТПРАВКА ПРОЕКТА НА GITHUB

### Этап 6.1: Добавление удаленного репозитория
```powershell
git remote add origin https://github.com/ВашеИмя/sber-auto-ml.git
```

### Этап 6.2: Проверка подключения
```powershell
git remote -v
```
**Результат:** Должны увидеть origin с вашим URL

### Этап 6.3: Отправка кода
```powershell
git push -u origin main
```

**Если ошибка про ветку:**
```powershell
git branch -M main
git push -u origin main
```

### Этап 6.4: Ввод учетных данных
- Откроется окно для ввода логина/пароля GitHub
- Или запросит токен доступа

---

## 🔷 ЧАСТЬ 7: СОЗДАНИЕ ТОКЕНА ДОСТУПА (если нужно)

### Этап 7.1: Создание токена
1. На GitHub: Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажмите "Generate new token"
3. Выберите "Generate new token (classic)"
4. Заполните:
   - **Note:** Git CLI access
   - **Expiration:** 90 days (рекомендуется)
   - **Select scopes:** Отметьте `repo` (полный доступ к репозиториям)
5. Нажмите "Generate token"
6. **Скопируйте токен** (он покажется только один раз!)

### Этап 7.2: Использование токена
При запросе пароля введите токен вместо пароля

---

## 🔷 ЧАСТЬ 8: ПРОВЕРКА И ОБНОВЛЕНИЕ

### Этап 8.1: Проверка на GitHub
1. Откройте https://github.com/ВашеИмя/sber-auto-ml
2. Убедитесь, что файлы загружены

### Этап 8.2: Обновление README (опционально)
Отредактируйте README.md чтобы он хорошо смотрелся на GitHub:

```markdown
# 🚗 SberAutoSubscription ML Project

## 📋 Описание
ML проект для предсказания конверсии в сервисе СберАвтоподписка.

## 🎯 Цель
Предсказать, совершит ли пользователь целевое действие (подписка, заявка и т.д.) на основе данных Google Analytics.

## 📊 Данные
- `ga_sessions.csv` - данные о визитах пользователей
- `ga_hits.csv` - данные о событиях на сайте

## 🏗️ Структура проекта
```
sber-auto-ml/
├── data/           # Данные (не включены в репозиторий)
├── notebooks/      # Jupyter ноутбуки
├── src/           # Исходный код
├── api/           # Flask API
├── models/        # Обученные модели (не включены)
└── tests/         # Тесты
```

## 🚀 Быстрый старт
1. Клонируйте репозиторий
2. Установите зависимости: `pip install -r requirements.txt`
3. Запустите обучение: `cd src && python model.py`
4. Запустите API: `cd api && python app.py`

## 📈 Результаты
- ROC-AUC: ~0.65
- Время ответа API: < 100мс
- Формат ответа: `{"prediction": 0|1, "probability": float}`
```

### Этап 8.3: Добавление изменений
```powershell
git add README.md
git commit -m "Update README for GitHub"
git push
```

---

## 🔷 ЧАСТЬ 9: ДОПОЛНИТЕЛЬНЫЕ ВОЗМОЖНОСТИ

### Этап 9.1: Добавление лицензии
1. На GitHub: Add file → Create new file
2. Имя файла: `LICENSE`
3. Выберите шаблон лицензии (например, MIT License)
4. Commit changes

### Этап 9.2: Добавление badges (значков)
В README.md можно добавить:

```markdown
![Python](https://img.shields.io/badge/python-3.8+-blue.svg)
![ML](https://img.shields.io/badge/ML-RandomForest-green.svg)
![API](https://img.shields.io/badge/API-Flask-red.svg)
```

### Этап 9.3: Настройка GitHub Pages (для документации)
1. Settings → Pages
2. Source: выберите ветку `main` и папку `/docs` (или root)
3. Save

---

## 🔷 ЧАСТЬ 10: РАБОТА С РЕПОЗИТОРИЕМ

### Основные команды Git:

```powershell
# Проверка статуса
git status

# Добавление файлов
git add <файл>
git add .  # все файлы

# Коммит изменений
git commit -m "Описание изменений"

# Отправка на GitHub
git push

# Получение обновлений
git pull

# Создание новой ветки
git checkout -b feature/new-feature

# Слияние веток
git merge feature/new-feature
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Не загружайте большие файлы** (CSV, модели) - они в .gitignore
2. **Токены и пароли** никогда не добавляйте в репозиторий
3. **Делайте частые коммиты** с понятными сообщениями
4. **Публичный репозиторий** виден всем - не добавляйте конфиденциальные данные

---

## ✅ ЧЕК-ЛИСТ ВЫПОЛНЕНИЯ

- [ ] Установлен Git
- [ ] Настроены имя и email в Git
- [ ] Создан .gitignore
- [ ] Создан репозиторий на GitHub
- [ ] Проект отправлен на GitHub
- [ ] Репозиторий публичный
- [ ] README обновлен

---

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ

- [Git Documentation](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)
- [Markdown Guide](https://www.markdownguide.org/)

Теперь ваш проект доступен по ссылке: `https://github.com/ВашеИмя/sber-auto-ml`
# Подробная инструкция по работе с проектом СберАвтоподписка ML

## Часть 0: Подготовка окружения

### Этап 0.1: Распаковка проекта
**Где:** Проводник Windows + PyCharm
**Действия:** Распакуйте архив, откройте папку в PyCharm
**Результат:** Проект открыт в PyCharm, видны папки data/, notebooks/, src/, api/, models/

### Этап 0.2: Подготовка данных
**Где:** Проводник Windows  
**Действия:** Поместите ga_sessions.csv и ga_hits.csv в папку sber-auto-ml/data/
**Результат:** Файлы данных на месте

### Этап 0.3: Создание виртуального окружения
**Где:** Терминал PyCharm
**Команда:** `python -m venv venv`
**Результат:** Создана папка venv/

### Этап 0.4: Активация окружения
**Где:** Терминал PyCharm
**Команда:** `venv\Scripts\Activate.ps1`
**Результат:** В начале строки появится (venv)

### Этап 0.5: Установка зависимостей
**Где:** Терминал PyCharm
**Команда:** `pip install -r requirements.txt`
**Результат:** Установлены все библиотеки

### Этап 0.6: Настройка интерпретатора
**Где:** PyCharm Settings
**Действия:** File → Settings → Project → Python Interpreter → выберите venv
**Результат:** Интерпретатор настроен

---

## Часть 1: EDA (Exploratory Data Analysis)

### Этап 1.1: Запуск Jupyter
**Где:** Терминал PyCharm
**Команда:** `jupyter notebook notebooks/`
**Результат:** Открылся браузер с Jupyter

### Этап 1.2: Открытие ноутбука
**Где:** Браузер
**Действия:** Кликните на 01_eda.ipynb
**Результат:** Открыт ноутбук с EDA

### Этап 1.3: Импорт библиотек
**Где:** Jupyter Notebook
**Код:** Выполните первую ячейку с импортами
**Результат:** Сообщение "Библиотеки загружены"

### Этап 1.4: Загрузка данных
**Где:** Jupyter Notebook
**Код:** Выполните ячейку с load_data
**Результат:** Вывод размеров датафреймов

### Этап 1.5: Очистка данных
**Где:** Jupyter Notebook
**Код:** Выполните ячейки с drop_duplicates, преобразованием дат
**Результат:** Данные очищены, дубликаты удалены

### Этап 1.6: Создание целевой переменной
**Где:** Jupyter Notebook
**Код:** Выполните ячейку с созданием target
**Результат:** Создана колонка target с 0/1

### Этап 1.7: Визуализация
**Где:** Jupyter Notebook
**Код:** Выполните ячейки с графиками
**Результат:** Построены графики распределений

### Этап 1.8: Корреляции
**Где:** Jupyter Notebook
**Код:** Выполните ячейку с heatmap
**Результат:** Тепловая карта корреляций

### Этап 1.9: Сохранение данных
**Где:** Jupyter Notebook
**Код:** Сохранение в .pkl файлы
**Результат:** Файлы в data/

---

## 🔄 ПЕРЕХОД ОТ EDA К FEATURE ENGINEERING

**После завершения EDA у вас есть 2 варианта:**

**Вариант 1: Продолжить в том же Jupyter Notebook (01_eda.ipynb)**
- Просто продолжайте выполнять ячейки в том же ноутбуке
- Все код для Feature Engineering уже есть в ноутбуке

**Вариант 2: Запустить скрипт обучения (рекомендуется)**
- Закройте Jupyter Notebook (Ctrl+C в терминале)
- Перейдите в терминал PyCharm:
```powershell
cd src
python model.py
```
- **Скрипт model.py автоматически использует:**
  - `data_loader.py` - для загрузки данных
  - `features.py` - для создания признаков
  - И выполняет весь процесс обучения и сохранения модели

---

## 📁 СТРУКТУРА СКРИПТОВ И ИХ ИСПОЛЬЗОВАНИЕ

### **src/data_loader.py**
- **Когда используется:** При запуске model.py или при ручной загрузке данных
- **Что делает:** Загружает данные из CSV/PKL файлов, создает целевую переменную
- **Функции:** `load_data()`, `create_target_variable()`

### **src/features.py**
- **Когда используется:** При запуске model.py или при создании признаков вручную
- **Что делает:** Создает признаки (временные, статистические), кодирует категории
- **Функции:** `create_features()`, `encode_categorical()`

### **src/model.py**
- **Когда используется:** Для автоматического обучения модели
- **Что делает:** Использует data_loader и features, обучает RandomForest, сохраняет модель
- **Запуск:** `python model.py` из папки src/

---

## Часть 2: Feature Engineering и обучение

### Этап 2.1: Запуск скрипта обучения (рекомендуемый путь)
**Где:** Терминал PyCharm
**Команда:**
```powershell
cd src
python model.py
```
**Что происходит:**
1. model.py вызывает data_loader.py для загрузки данных
2. model.py вызывает features.py для создания признаков
3. Автоматически выполняется весь процесс обучения
**Результат:** Модель обучена и сохранена в models/

### Этап 2.2: Создание признаков (через features.py)
**Где:** Скрипт src/model.py автоматически ИЛИ вручную в Jupyter
**Используется:** src/features.py → функция create_features()
**Что делает:** Создает visit_dayofweek, is_weekend, total_hits, unique_events
**Результат:** Новые колонки в датафрейме

### Этап 2.3: Кодирование категориальных признаков (через features.py)
**Где:** Скрипт src/model.py автоматически ИЛИ вручную в Jupyter
**Используется:** src/features.py → функция encode_categorical()
**Что делает:** LabelEncoder для utm_source, utm_medium, device_category, device_os, geo_country, geo_city
**Результат:** Созданы *_encoded колонки (например, utm_source_encoded)

### Этап 2.4: Подготовка данных для обучения
**Где:** Скрипт src/model.py автоматически ИЛИ вручную в Jupyter
**Код:** Выбор финальных признаков для модели
**Результат:** Список feature_cols (12+ признаков)

### Этап 2.5: Разделение на train/test
**Где:** Скрипт src/model.py автоматически ИЛИ вручную в Jupyter
**Код:** train_test_split с test_size=0.2, random_state=42, stratify=y
**Результат:** X_train, X_test, y_train, y_test (примерно 120k/30k записей)

### Этап 2.6: Обучение модели RandomForest
**Где:** Скрипт src/model.py автоматически ИЛИ вручную в Jupyter
**Код:** RandomForestClassifier(n_estimators=100, max_depth=10, random_state=42)
**Результат:** Обученная модель, время ~30-60 секунд

### Этап 2.7: Оценка качества модели
**Где:** Скрипт src/model.py автоматически ИЛИ вручную в Jupyter
**Код:** roc_auc_score, classification_report, confusion_matrix
**Результат:** ROC-AUC ~0.65, отчет о классификации, матрица ошибок

### Этап 2.8: Feature Importance (важность признаков)
**Где:** Скрипт src/model.py автоматически ИЛИ вручную в Jupyter
**Код:** model.feature_importances_, построение графика
**Результат:** График важности признаков, топ-10 самых важных

### Этап 2.9: Сохранение модели и артефактов
**Где:** Скрипт src/model.py автоматически ИЛИ вручную в Jupyter
**Код:** joblib.dump для сохранения модели, кодировщиков и списка признаков
**Результат:** 3 файла в папке models/:
- models/model.pkl (обученная модель)
- models/encoders.pkl (LabelEncoder'ы для категорий)
- models/feature_cols.pkl (список признаков для API)

---

## Часть 3: API

### Этап 3.1: Переход в папку api
**Где:** Терминал PyCharm
**Команда:** `cd api`
**Результат:** Вы в папке api

### Этап 3.2: Запуск API
**Где:** Терминал PyCharm
**Команда:** `python app.py`
**Результат:** Сервер запущен на http://0.0.0.0:5000

### Этап 3.3: Проверка API
**Где:** Новый терминал
**Команда:** `curl http://localhost:5000/health`
**Результат:** {"status": "healthy"}

### Этап 3.4: Тест предсказания
**Где:** Терминал PowerShell

**Правильный формат запроса (добавьте visit_number):**

```powershell
Invoke-RestMethod -Uri "http://localhost:5000/predict" -Method Post -Body '{"utm_source": "google", "utm_medium": "cpc", "device_category": "mobile", "device_os": "Android", "geo_country": "Russia", "geo_city": "Moscow", "visit_number": 1}' -ContentType "application/json"
```

**Или более подробный вариант:**

```powershell
$body = @{
    utm_source = "google"
    utm_medium = "cpc"
    device_category = "mobile"
    device_os = "Android"
    geo_country = "Russia"
    geo_city = "Moscow"
    visit_number = 1
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:5000/predict" `
    -Method Post `
    -ContentType "application/json" `
    -Body $body
```

**Если у вас установлен curl (Windows 10+):**

```powershell
curl.exe -X POST http://localhost:5000/predict `
  -H "Content-Type: application/json" `
  -d '{"utm_source": "google", "utm_medium": "cpc", "device_category": "mobile", "device_os": "Android", "geo_country": "Russia", "geo_city": "Moscow", "visit_number": 1}'
```

**Примечание:** API теперь автоматически добавляет `visit_number=1` если его нет, но лучше передавать явно.

**Результат:**
```json
{
  "prediction": 1,
  "probability": 0.72,
  "will_convert": true
}
```

или

```json
{
  "prediction": 0,
  "probability": 0.23,
  "will_convert": false
}
```

### Этап 3.5: Остановка
**Где:** Терминал с сервером
**Действие:** Ctrl+C
**Результат:** Сервер остановлен

---

## ✅ ДОСТИЖЕНИЕ ЦЕЛИ ПРОЕКТА

**Цель проекта:** Модель, берущая на вход строку с данными по визиту и отдающая на выход результат предсказания по отдельному событию в числовом формате 0|1.

**✅ Цель достигнута:**

1. **Входные данные:** API принимает JSON с данными визита:
   ```json
   {
     "utm_source": "google",
     "utm_medium": "cpc", 
     "device_category": "mobile",
     "device_os": "Android",
     "geo_country": "Russia",
     "geo_city": "Moscow"
   }
   ```

2. **Выходные данные:** API возвращает предсказание в формате 0|1:
   ```json
   {
     "prediction": 1,
     "probability": 0.72,
     "will_convert": true
   }
   ```
   - `prediction`: 0 (не конвертируется) или 1 (конвертируется)
   - `probability`: вероятность конверсии (0.0-1.0)
   - `will_convert`: булево значение для удобства

3. **Схема данных соответствует:** Используются те же поля, что и в исходных данных GA:
   - utm_source, utm_medium, device_category, device_os, geo_country, geo_city
   - Плюс автоматически вычисляемые признаки (visit_dayofweek, is_weekend и др.)

4. **Производительность:** ROC-AUC ~0.65, что соответствует требованиям

5. **Время ответа API:** < 3 секунд (обычно < 100мс)

---

## Чек-лист

- [ ] Часть 0: Окружение настроено
- [ ] Часть 1: EDA выполнен
- [ ] Часть 2: Модель обучена (ROC-AUC ~0.65)
- [ ] Часть 3: API работает и возвращает 0|1
- [ ] Цель проекта достигнута: модель принимает данные визита → возвращает 0|1
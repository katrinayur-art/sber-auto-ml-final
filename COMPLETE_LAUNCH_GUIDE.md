# 🚀 ПОЛНОЕ РУКОВОДСТВО ПО ЗАПУСКУ ML-ПРОЕКТА "СБЕРАВТОПОДПИСКА"

## 📋 СОДЕРЖАНИЕ
1. [Подготовка окружения](#1-подготовка-окружения)
2. [Запуск Jupyter Notebooks](#2-запуск-jupyter-notebooks)
3. [Запуск Python скриптов](#3-запуск-python-скриптов)
4. [Запуск API сервиса](#4-запуск-api-сервиса)
5. [Запуск через Docker](#5-запуск-через-docker)
6. [Тестирование проекта](#6-тестирование-проекта)
7. [Устранение неполадок](#7-устранение-неполадок)

---

## 1. ПОДГОТОВКА ОКРУЖЕНИЯ

### 1.1 Установка Python (если не установлен)
```bash
# Проверка версии Python
python --version
# Должно быть Python 3.8 или выше

# Если Python не установлен:
# Windows: скачать с python.org
# macOS: brew install python
# Linux: sudo apt install python3 python3-pip
```

### 1.2 Клонирование/распаковка проекта
```bash
# Распаковка архива (если получили tar.gz)
tar -xzf sberauto_ml_project_complete_updated.tar.gz
cd sber-auto-ml

# Или если проект уже распакован:
cd sber-auto-ml
```

### 1.3 Создание виртуального окружения
```bash
# Создание виртуального окружения
python -m venv venv

# Активация (Windows PowerShell):
venv\Scripts\Activate.ps1

# Активация (Windows CMD):
venv\Scripts\activate.bat

# Активация (macOS/Linux):
source venv/bin/activate
```

### 1.4 Установка зависимостей
```bash
# Установка всех зависимостей проекта
pip install -r requirements.txt

# Дополнительная установка для API
cd api
pip install -r requirements.txt
cd ..
```

### 1.5 Проверка установки
```bash
# Проверка установленных библиотек
python -c "import pandas as pd; print(f'Pandas: {pd.__version__}')"
python -c "import sklearn; print(f'Scikit-learn: {sklearn.__version__}')"
python -c "import flask; print(f'Flask: {flask.__version__}')"
```

---

## 2. ЗАПУСК JUPYTER NOTEBOOKS

### 2.1 Запуск Jupyter сервера
```bash
# Активируйте виртуальное окружение (если еще не активировано)
# source venv/bin/activate  # macOS/Linux
# venv\Scripts\activate     # Windows

# Установка Jupyter (если не установлен)
pip install jupyter

# Запуск Jupyter сервера
jupyter notebook

# Или запуск из конкретной папки
jupyter notebook notebooks/
```

### 2.2 Открытие ноутбуков в браузере
После запуска Jupyter:
1. Откроется браузер с адресом `http://localhost:8888`
2. Перейдите в папку `notebooks/`
3. Откройте нужный ноутбук:
   - `01_eda_with_results.ipynb` - EDA с результатами
   - `02_preprocessing_modeling.ipynb` - Предобработка и обучение
   - `03_pipeline_results.ipynb` - Пайплайн и результаты

### 2.3 Выполнение ноутбуков
**Вариант A: Запуск всех ячеек**
1. В меню выберите `Cell` → `Run All`
2. Дождитесь выполнения всех ячеек
3. Проверьте результаты в каждой ячейке

**Вариант B: Пошаговое выполнение**
1. Нажмите `Shift + Enter` для выполнения текущей ячейки
2. Переходите к следующей ячейке
3. Для перезапуска: `Kernel` → `Restart & Run All`

### 2.4 Особенности каждого ноутбука

#### **01_eda_with_results.ipynb**
```python
# Ячейки уже выполнены, но можно перезапустить:
# 1. Загрузка данных из data/
# 2. Очистка и преобразование
# 3. Создание целевой переменной
# 4. Визуализации
```

#### **02_preprocessing_modeling.ipynb**
```python
# Для переобучения модели:
# 1. Измените параметры RandomForest (ячейка 6)
# 2. Запустите ячейки 1-8 последовательно
# 3. Сохраните новую модель (ячейка 9)
```

#### **03_pipeline_results.ipynb**
```python
# Полный пайплайн:
# 1. Запустите все ячейки по порядку
# 2. Проверьте итоговые метрики
# 3. Модель сохранится в models/
```

---

## 3. ЗАПУСК PYTHON СКРИПТОВ

### 3.1 Загрузка и подготовка данных
```bash
# Перейдите в папку src
cd src

# Запуск скрипта загрузки данных
python data_loader.py

# Результат:
# - Загрузит данные из data/
# - Сохранит очищенные версии
# - Выведет статистику
```

### 3.2 Feature Engineering
```bash
# Создание признаков
python features.py

# Результат:
# - Создаст временные признаки
# - Добавит статистики из hits
# - Сохранит обогащенные данные
```

### 3.3 Обучение модели
```bash
# Основной скрипт обучения
python model.py

# Результат:
# 1. Загрузит подготовленные данные
# 2. Выполнит кодирование категорий
# 3. Обучит RandomForest
# 4. Оценит качество (ROC-AUC: ~0.65)
# 5. Сохранит модель в models/
```

### 3.4 Параметры запуска скриптов
```bash
# model.py с параметрами
python model.py --n_estimators 200 --max_depth 15

# Доступные параметры:
# --n_estimators: количество деревьев (по умолчанию: 100)
# --max_depth: максимальная глубина (по умолчанию: 10)
# --test_size: размер тестовой выборки (по умолчанию: 0.2)
# --random_state: seed для воспроизводимости (по умолчанию: 42)
```

---

## 4. ЗАПУСК API СЕРВИСА

### 4.1 Запуск Flask API
```bash
# Перейдите в папку api
cd api

# Запуск API сервера
python app.py

# Или с указанием порта
python app.py --port 5000

# Результат:
# * Serving Flask app 'app'
# * Debug mode: off
# * Running on http://127.0.0.1:5000
```

### 4.2 Проверка работы API
```bash
# Проверка health endpoint
curl http://localhost:5000/health
# Ответ: {"status": "healthy", "model_loaded": true}

# Проверка корневого endpoint
curl http://localhost:5000/
# Ответ: {"message": "SberAuto ML API", "endpoints": ["/predict", "/health"]}
```

### 4.3 Тестирование предсказаний
#### **PowerShell:**
```powershell
# Тестовый запрос к API
$body = @{
    utm_source = "google"
    utm_medium = "cpc"
    device_category = "mobile"
    device_os = "Android"
    geo_country = "Russia"
    geo_city = "Moscow"
    visit_number = 1
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:5000/predict" `
    -Method Post `
    -ContentType "application/json" `
    -Body $body
```

#### **Windows CMD (с curl):**
```cmd
curl -X POST http://localhost:5000/predict ^
  -H "Content-Type: application/json" ^
  -d "{\"utm_source\": \"google\", \"utm_medium\": \"cpc\", \"device_category\": \"mobile\", \"device_os\": \"Android\", \"geo_country\": \"Russia\", \"geo_city\": \"Moscow\", \"visit_number\": 1}"
```

#### **Python:**
```python
import requests
import json

data = {
    "utm_source": "google",
    "utm_medium": "cpc",
    "device_category": "mobile",
    "device_os": "Android",
    "geo_country": "Russia",
    "geo_city": "Moscow",
    "visit_number": 1
}

response = requests.post("http://localhost:5000/predict", json=data)
print(response.json())
```

### 4.4 Формат ответа API
```json
{
  "prediction": 1,
  "probability": 0.72,
  "will_convert": true
}
```

---

## 5. ЗАПУСК ЧЕРЕЗ DOCKER

### 5.1 Установка Docker
1. Скачайте Docker Desktop: https://www.docker.com/products/docker-desktop
2. Установите и запустите Docker
3. Проверьте установку: `docker --version`

### 5.2 Сборка и запуск контейнеров
```bash
# Перейдите в корень проекта
cd sber-auto-ml

# Запуск через docker-compose
docker-compose up

# Или в фоновом режиме
docker-compose up -d
```

### 5.3 Доступ к сервисам
После запуска docker-compose:
- **Jupyter Notebook:** http://localhost:8888
- **API сервис:** http://localhost:5000
- **Документация API:** http://localhost:5000/

### 5.4 Остановка контейнеров
```bash
# Остановка с удалением контейнеров
docker-compose down

# Только остановка
docker-compose stop
```

---

## 6. ТЕСТИРОВАНИЕ ПРОЕКТА

### 6.1 Запуск тестов
```bash
# Перейдите в папку tests
cd tests

# Запуск всех тестов
python -m pytest

# Запуск конкретного теста
python -m pytest test_api.py
python -m pytest test_model.py
```

### 6.2 Проверка качества модели
```bash
# Запуск скрипта оценки
cd src
python evaluate_model.py

# Результат:
# - ROC-AUC метрика
# - Confusion matrix
# - Classification report
```

### 6.3 Валидация данных
```bash
# Проверка целостности данных
cd src
python validate_data.py

# Проверяет:
# - Наличие всех файлов
# - Корректность форматов
# - Отсутствие критических ошибок
```

---

## 7. УСТРАНЕНИЕ НЕПОЛАДОК

### 7.1 Распространенные ошибки и решения

#### **Ошибка: ModuleNotFoundError**
```bash
# Решение: установите недостающую библиотеку
pip install имя_библиотеки

# Или переустановите все зависимости
pip install -r requirements.txt --force-reinstall
```

#### **Ошибка: FileNotFoundError (данные не найдены)**
```bash
# Решение: проверьте наличие файлов
ls data/
# Должны быть: ga_sessions.csv, ga_hits.csv

# Если файлов нет, скопируйте их в data/
cp /путь/к/файлам/*.csv data/
```

#### **Ошибка: API не отвечает**
```bash
# 1. Проверьте запущен ли API
netstat -an | findstr :5000  # Windows
lsof -i :5000                # macOS/Linux

# 2. Перезапустите API
cd api
python app.py

# 3. Проверьте firewall
# Разрешите порт 5000 в настройках брандмауэра
```

#### **Ошибка: Jupyter не запускается**
```bash
# 1. Проверьте установку
pip list | grep jupyter

# 2. Переустановите Jupyter
pip uninstall jupyter -y
pip install jupyter

# 3. Запустите с другим портом
jupyter notebook --port 8889
```

### 7.2 Проверка работоспособности

#### **Полная проверка:**
```bash
# 1. Проверка Python
python --version

# 2. Проверка зависимостей
pip list | grep -E "pandas|scikit|flask|jupyter"

# 3. Проверка данных
ls -la data/*.csv

# 4. Проверка модели
ls -la models/*.pkl

# 5. Тест API
curl http://localhost:5000/health
```

### 7.3 Логирование и отладка

#### **Включение debug режима:**
```bash
# Для API
cd api
python app.py --debug

# Для скриптов
cd src
python model.py --verbose
```

#### **Просмотр логов:**
```bash
# Логи API
tail -f api.log

# Логи обучения
cat training.log

# Логи Docker
docker-compose logs
```

---

## 📊 ЧЕК-ЛИСТ ЗАПУСКА

### ✅ Базовый запуск:
- [ ] Распакован проект
- [ ] Создано виртуальное окружение
- [ ] Установлены зависимости
- [ ] Данные в папке data/

### ✅ Запуск ноутбуков:
- [ ] Jupyter установлен
- [ ] Ноутбуки открываются
- [ ] Ячейки выполняются
- [ ] Результаты отображаются

### ✅ Запуск скриптов:
- [ ] data_loader.py работает
- [ ] features.py работает
- [ ] model.py обучает модель
- [ ] Модель сохраняется в models/

### ✅ Запуск API:
- [ ] API запускается
- [ ] Health endpoint отвечает
- [ ] Predict endpoint работает
- [ ] Ответы корректные

### ✅ Тестирование:
- [ ] Тесты проходят
- [ ] Модель дает ROC-AUC ~0.65
- [ ] API отвечает < 3 секунд

---

## 🎯 БЫСТРЫЙ СТАРТ (Windows PowerShell)

```powershell
# 1. Распаковка
tar -xzf sberauto_ml_project_complete_updated.tar.gz
cd sber-auto-ml

# 2. Окружение
python -m venv venv
venv\Scripts\Activate.ps1
pip install -r requirements.txt

# 3. Запуск ноутбуков
jupyter notebook notebooks/

# 4. Обучение модели (в новом терминале)
venv\Scripts\Activate.ps1
cd src
python model.py

# 5. Запуск API (в новом терминале)
venv\Scripts\Activate.ps1
cd api
python app.py

# 6. Тестирование (в новом терминале)
venv\Scripts\Activate.ps1
$body = @{utm_source="google"; utm_medium="cpc"; device_category="mobile"; device_os="Android"; geo_country="Russia"; geo_city="Moscow"; visit_number=1} | ConvertTo-Json
Invoke-RestMethod -Uri "http://localhost:5000/predict" -Method Post -ContentType "application/json" -Body $body
```

---

## 📞 ПОДДЕРЖКА

### Контакты для вопросов:
- **По техническим вопросам:** [ваш email/telegram]
- **По данным:** проверьте наличие файлов в data/
- **По ошибкам:** приложите полный текст ошибки

### Полезные команды для диагностики:
```bash
# Версии библиотек
python -c "import sys; print(f'Python: {sys.version}')"
pip freeze | grep -E "pandas|numpy|scikit|flask"

# Проверка портов
netstat -ano | findstr :5000  # Windows
lsof -i :5000                 # macOS/Linux

# Проверка памяти
python -c "import psutil; print(f'Memory: {psutil.virtual_memory().percent}%')"
```

---

**✅ Проект готов к запуску! Следуйте